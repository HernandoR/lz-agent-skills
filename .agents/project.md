# Skills Repository Metadata

`.agents/` contains metadata about this repository only.

Installable skill packages live in top-level `skills/`, not here. Do not mirror
skills into `.agents/skills` unless a future RFC changes the repository layout.

